---
import { icons, type LucideIcon } from 'lucide-react'

type Props = {
	name: string
}

const { name } = Astro.props

let MyIcon: LucideIcon | null = null

if (name in icons) {
	MyIcon = icons[name as keyof typeof icons]
}
---

{MyIcon && <MyIcon />}
{!MyIcon && <span>{Astro.props.name}</span>}
